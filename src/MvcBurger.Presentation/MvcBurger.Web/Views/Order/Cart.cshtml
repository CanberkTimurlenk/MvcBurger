@using MvcBurger.Web.Models.VMs;
@model GetUserCartVM
@{
    Layout = "_Order";
}

    <h2>Shopping Cart</h2>

    <table class="table">
        <thead>
            <tr>
                <th>Product</th>
                <th>Price</th>
                <th>Quantity</th>
                <th>Total</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Cart.CartItems != null)
            {
                @foreach (var cartItem in Model.Cart.CartItems)
                {
                    <tr>
                        <td class="text-white">@cartItem.Menu.Name</td>
                    <td class="text-white">@cartItem.Menu.Price</td>
                    <td class="text-white">@cartItem.Amount"</td>
                    <td class="text-white">@(cartItem.Menu.Price * cartItem.Amount)</td>
                        <td>
                        <button type="button" class="btn btn-primary" asp-action="OrderDetail" asp-controller="Order" asp-route-id="@cartItem.Id">Update</button>
                        <button type="button" class="btn btn-danger" asp-action="OrderDetail" asp-controller="Order" asp-route-id="@cartItem.Id">Remove</button>
                        </td>
                    </tr>
                }

            }
            else
            {
                <tr>
                    <td>Henuz Menu Secmediniz</td>
                </tr>
            }
        </tbody>
    </table>

    <div class="text-right">
    <h4 class="text-white">Total: @Model.Cart.TotalPrice.ToString()</h4>
        <button type="button" class="btn btn-success">Checkout</button>
    </div>


